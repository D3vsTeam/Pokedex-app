import React from "react";
import { View } from "react-native";
import {Head} from '../../components'

export const Home = () => {
    return(
        <View style={{flex:1}}>
            <Head/>
        </View>
    )
}